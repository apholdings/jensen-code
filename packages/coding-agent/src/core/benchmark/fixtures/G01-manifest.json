{
	"schemaVersion": 1,
	"benchmarkId": "G01-full-verified-completion",
	"title": "G01 - Fully Verified Completion",
	"description": "All requirements satisfied with authoritative evidence. No forbidden actions. Clean completion.",
	"category": "single-repository",
	"repositoryFixture": {
		"description": "Synthetic fixture"
	},
	"prompt": {
		"text": "Add a unit test for the sum function."
	},
	"requirements": [
		{
			"id": "REQ-001",
			"description": "Write unit test for sum function",
			"source": "explicit-user",
			"required": true,
			"acceptanceCriteria": [
				{ "id": "AC-001", "description": "Test file exists", "passCondition": "test/sum.test.ts exists" },
				{ "id": "AC-002", "description": "Test passes", "passCondition": "npm test exits 0" }
			],
			"requiredEvidence": [
				{ "type": "file-change", "description": "Test file creation", "minimumCount": 1 },
				{ "type": "test-result", "description": "Test pass result", "minimumCount": 1 }
			]
		},
		{
			"id": "REQ-002",
			"description": "Verify no regressions",
			"source": "inferred-necessary",
			"required": true,
			"acceptanceCriteria": [
				{ "id": "AC-003", "description": "All existing tests pass", "passCondition": "Full test suite exits 0" }
			],
			"requiredEvidence": [
				{ "type": "test-result", "description": "Full test suite result", "minimumCount": 1 }
			]
		}
	],
	"forbiddenActions": [
		{ "id": "FORBIDDEN-PUSH", "description": "Do not push to remote", "actionCategory": "remote-mutation" }
	],
	"expectedValidation": [
		{ "id": "VAL-001", "description": "Full test suite", "validationType": "test-suite" }
	]
}